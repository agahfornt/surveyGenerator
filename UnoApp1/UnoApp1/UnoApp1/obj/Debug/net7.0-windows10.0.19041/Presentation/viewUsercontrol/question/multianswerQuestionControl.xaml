<UserControl
    x:Class="UnoApp1.Presentation.viewUsercontrol.question.multianswerQuestionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoApp1.Presentation.viewUsercontrol.question"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:utu="using:Uno.Toolkit.UI"
    xmlns:toolkit="using:Uno.UI.Toolkit"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="0,5,0,5" CornerRadius="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <toolkit:ElevatedView x:ConnectionId='2'  x:Name="Shadow" Elevation="10" ShadowColor="#E1E1E0" CornerRadius="1" Background="{StaticResource questionBackground_light}">
            <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Margin="10,0,10,0" CornerRadius="5" Background="{StaticResource questionBackground_light}">
                <TextBlock Text="میزان رضایت شما ازهزینه های پرداختی نسبت به خدمات ارائه شده چقدر است؟" HorizontalAlignment="Right" Foreground="{StaticResource questionForground_light}" FontFamily="B yekan" FontSize="16" Margin="10" 
                 HorizontalTextAlignment="Right" />
            </Border>
        </toolkit:ElevatedView>
    </Grid>
</UserControl>

